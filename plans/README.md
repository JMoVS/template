# Active plans

Keep plans here only while they describe active work. Create from `templates/plan.md`; use stable work IDs in the filename. Archive under `plans/archive/` when the project's completion rule is satisfied. Archived plans provide durable context after active backlog headings disappear.
