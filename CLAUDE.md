@AGENTS.md

Read `docs/project.md` for project-specific commands and authority. Load the relevant role instructions when delegating. Agent configuration lives in `.claude/agents/`; those wrappers do not enforce model-tier or merge policy.
